# .github/workflows/test-email.yml

name: Test Email
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 📧 Envoyer email de test
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.SMTP_SERVER }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          from: ${{ secrets.EMAIL_FROM }}
          to: ${{ secrets.EMAIL_TO_ALL }}
          subject: "🧪 Test CI/CD — Email fonctionne !"
          html_body: |
            <h2 style="color:green">✅ Ça marche !</h2>
            <p>L'email CI/CD est bien configuré pour le repo Salesforce.</p>
